# Port the HTTP + Socket.IO server listens on.
PORT=3001

# Comma-separated browser origins allowed to connect (CORS + Socket.IO).
# Set this to your deployed front end's origin in production; the defaults
# below only cover local development.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
